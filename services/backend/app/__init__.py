# ===========================================================================
# services/backend/app/__init__.py
# ---------------------------------------------------------------------------
# Package initializer for the FastAPI backend application.
#
# This file marks the `app` directory as a Python package so that modules
# like `main`, `db`, `config`, `api_models`, `degradation`,
# `recommendation_engine`, and `gap_engine` can be imported with relative
# imports (e.g., `from .db import get_conn`).
#
# No exports are defined here; each module is imported directly where needed.
# ===========================================================================
